namespace GameOfLife
{
    public class World
    {
        // contains main method
        // parent class for screen and cell classes
        // defines grid properties - i.e. width and height, and an array of cells
        // contains method called nextState that takes a cell instance and checks the isAlive value of that cells neighbors
        // returns an array of cells with new isAlive values to screen class
        // will need to account for literal edge and corner cases here
    }
}